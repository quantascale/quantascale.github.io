baseURL = 'http://example.org/'
languageCode = 'en-us'
title = 'QuantaScale blog'
#theme = "terminal"
theme = "hugo-bearcub"

enableRobotsTXT = true

author = "Jane Doe"
copyright = "Copyright © 2023, Thilanka Liyanaarachchi."

# Generate "Bearblog"-like URLs !only!, see https://bearblog.dev/.
# disableKinds = ["taxonomy"]
# ignoreErrors = ["error-disable-taxonomy"]
# Setup syntax highlighting without inline styles. For more information about
# why you'd want to avoid inline styles, see
# https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/style-src#unsafe_inline_styles
[markup]
  [markup.highlight]
    lineNos = true
    lineNumbersInTable = false
    # This allows Bear Cub to use a variation of Dracula that is more accessible
    # to people with poor eyesight. For more information about color contrast
    # and accessibility, see https://web.dev/color-and-contrast-accessibility/
    noClasses = false

# Social media. Delete any item you aren't using to make sure it won't show up
  # in your website's metadata.
  [social]
    email = "catchthila@gmail.com" # Added to the navbar so readers can reply to posts
    # twitter = "example" # Twitter handle (without '@')
    # facebook_admin = "0000000000" # Facebook Page Admin ID


[params]
  # The description of your website
  description = "Personal website of Thilanka Liyanaarachchi"
   # This title is used as the site_name on the Hugo's internal opengraph
  # structured data template
  title = "QuantaScale"
